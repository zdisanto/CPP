// CODECADEMY >> C++ >> Chapter 6 >> Functions >> Void — The Point of No Return
/*
  1. Above main(), define a void function oscar_wilde_quote() that prints “The highest, as the lowest, form of criticism is a mode of autobiography.” to the output terminal.
  2. Inside of main(), call oscar_wilde_quote().
*/

#include <iostream>

// Define oscar_wilde_quote() below:
void oscar_wilde_quote() {
    std::cout << "The highest, as the lowest, form of criticism is a mode of autobiography\n";
}

int main() {
    // Call your function here:
    oscar_wilde_quote();
}